
export const codes = {
  SUCCESS: 200
}

export const defaults = {
  // withCredentials: true
}

export const requestDefaults = {
  autoLoading: true,
  // server端请求msg
  msgKey: 'error_msg',
  // server端数据的key
  dataKey: 'data',
  // server端请求状态的key
  codeKey: 'dm_error',
  // 浏览器请求成功的状态
  successNo: 0 
}